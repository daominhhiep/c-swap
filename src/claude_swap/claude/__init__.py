"""Claude Code account management: credentials, OAuth, switching, sessions.

Everything specific to Claude Code lives here; ``claude_swap.codex`` is the
Codex CLI counterpart, and the shared plumbing (paths, locking, usage cache,
JSON output, TUI) stays at the package root.
"""
